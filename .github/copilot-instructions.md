# Copilot Instructions — geonode-mapstore-client

## Overview

GeoNode MapStore Client is the frontend for **GeoNode**, an open-source geospatial content management system. It is a ReactJS-based MapStore2 downstream project packaged as a Django (Python) pip module (`django-geonode-mapstore-client`). The repo contains both the JavaScript client application and a thin Django integration layer (views, templates, templatetags, migrations).

**Current version:** 5.0.0.dev0 (branch `5.0.x`).

## Key Technologies

- **Frontend:** React, Redux, webpack 5, Less (CSS), ES modules
- **Backend:** Django ≥ 4.2, < 5.3 (Python 3.x)
- **Runtime:** Node 20.13.1, npm 10.5.2 (use `nvm use 20.13.1` — **always use Node 20**)
- **MapStore2** is a **git submodule** at `geonode_mapstore_client/client/MapStore2` (fork: `MarkBremer/MapStore2`, branch `geonode-5.0.x`)
- **ESLint** config is declared inline in `package.json` (extends `@mapstore/eslint-config-mapstore`); no separate `.eslintrc` file exists
- `.npmrc` sets `legacy-peer-deps=true`

## Repository Layout

```
/                                    # Repo root (Django package metadata)
├── Makefile                         # Top-level make targets: build, geonode, package, release
├── setup.py / setup.cfg             # Python package config; flake8 config in [flake8] section of setup.cfg
├── requirements.txt                 # Python deps (django, requests, Markdown, etc.)
├── VERSION                          # Single-line version string
├── geonode_mapstore_client/         # Django app root
│   ├── views.py                     # Django views
│   ├── models.py, admin.py          # Django models, admin
│   ├── templates/                   # Django templates (Jinja2-style)
│   │   └── geonode-mapstore-client/ # Main client templates (_geonode_config.html, catalogue.html, etc.)
│   ├── templatetags/                # Custom Django template tags
│   ├── static/mapstore/             # **Compiled** client output committed to repo
│   │   ├── configs/                 # Runtime JSON configs (localConfig.json, pluginsConfig.json, etc.)
│   │   ├── dist/                    # Webpack bundles (generated by `npm run compile`)
│   │   ├── gn-translations/        # GeoNode i18n
│   │   └── ms-translations/        # MapStore i18n
│   ├── migrations/                  # Django migrations
│   └── client/                      # **Frontend source root** (all JS/CSS development happens here)
│       ├── package.json             # npm scripts, deps, eslint config, mapstore project config
│       ├── jsconfig.json            # Path aliases: @mapstore/framework/*, @js/*
│       ├── postCompile.js           # Post-compile hook (copies dist → static/mapstore)
│       ├── devServer.js             # Webpack dev server config (reads .env)
│       ├── build.sh                 # Shell helper for install + compile combos
│       ├── MapStore2/               # Git submodule — upstream MapStore2 framework
│       ├── js/                      # Application source code
│       │   ├── apps/               # Entry points: gn-catalogue, gn-map, gn-dashboard, gn-document, gn-geostory, gn-components
│       │   ├── plugins/            # GeoNode-specific MapStore plugins (index.js registers all)
│       │   ├── components/         # React components
│       │   ├── epics/              # Redux-observable epics
│       │   ├── reducers/           # Redux reducers
│       │   ├── selectors/          # Redux selectors
│       │   ├── actions/            # Redux action creators
│       │   ├── api/                # API clients (geonode/v2/)
│       │   ├── hooks/              # React hooks
│       │   ├── routes/             # Route components
│       │   └── utils/              # Utility modules
│       └── themes/geonode/less/    # Less theme files (_variables.less, theme.less, etc.)
├── locale/                          # Django i18n (en, it)
└── tutorials/                       # Developer tutorials (markdown)
```

### Import Path Aliases (jsconfig.json)

- `@mapstore/framework/*` → `./node_modules/mapstore/web/client/*`
- `@js/*` → `./js/*`

## Build & Validation — Always Do These Steps

**All frontend commands must be run from `geonode_mapstore_client/client/`.**

### 1. Prerequisites (always run first)

```bash
cd geonode_mapstore_client/client
# If nvm is available (it is on most CI/dev machines):
nvm use 20.13.1
# Ensure submodule is initialized:
git submodule update --init --recursive
# Install dependencies (always run before any other npm command):
npm install
```

### 2. Lint (ESLint with auto-fix)

```bash
npm run lint
```

- Runs: `eslint js --ext .jsx,.js --fix`
- **Known pre-existing failure:** `js/plugins/SaveAs.jsx` line 115 has `'dataCy' is defined but never used` (`no-unused-vars`). This causes exit code 1. Do not introduce new lint errors.

### 3. Tests (Karma + webpack)

```bash
npm test
```

- Runs: `mapstore-project test` (Karma test runner with webpack bundling)
- **Known issue:** Tests currently fail with a webpack `NormalModuleFactory.resolve` hook error in the test runner. This is a pre-existing upstream compatibility issue and is not caused by local changes.
- Test files follow the pattern `__tests__/*-test.{js,jsx}` alongside source files (32 test files total).

### 4. Compile (production build)

```bash
npm run compile
```

- Runs: `mapstore-project compile && node postCompile`
- Takes approximately **3–4 minutes** (~216 seconds). Be patient.
- Outputs to `dist/`, then `postCompile.js` moves files into `static/mapstore/dist/` and `static/mapstore/ms-translations/`.
- **This modifies committed files** in `geonode_mapstore_client/static/mapstore/`. After compiling, these changes should be committed.

### 5. Development server (for local development only)

```bash
cp .env.sample .env   # then edit DEV_TARGET_GEONODE_HOST
npm start
```

Opens at `http://localhost:8081/` and proxies API calls to the GeoNode host defined in `.env`.

## CI Workflows (`.github/workflows/`)

| Workflow | Trigger | What it does |
|----------|---------|-------------|
| `build.yml` | Push to `5.0.x` (when `VERSION` or `client/**` change) | `npm install` → `npm run compile` → creates PR with compiled output |
| `test.yml` | PR to `5.0.x` (when `client/**` changes) | `npm install` → `npm run lint` → `npm test` |
| `static.yml` | Push to `5.0.x` | Builds JSDoc documentation and deploys to GitHub Pages |

All workflows use **Node 20** and run `git submodule update --init --recursive` first.

## Key Conventions

- **Plugin registration:** All plugins are exported from `js/plugins/index.js`. New plugins must be added there. The file uses a custom `toModulePlugin` wrapper that filters out excluded epics.
- **App entry points:** Each file in `js/apps/` (e.g., `gn-catalogue.js`) is a standalone webpack entry. They import plugins from `js/plugins/index.js` and configure routes, reducers, and epics.
- **Themes:** Less files in `themes/geonode/less/`. The main entry is `theme.less` which imports partials prefixed with `_`.
- **Static configs:** JSON files in `static/mapstore/configs/` (`localConfig.json`, `pluginsConfig.json`) control runtime MapStore behavior.
- **Django templates:** `templates/geonode-mapstore-client/_geonode_config.html` is the central template that bridges Django settings to the JS client via `<script>` blocks.
- **Translations:** GeoNode translations are in `static/mapstore/gn-translations/`; MapStore translations in `static/mapstore/ms-translations/` (auto-copied during compile).

## cy-data Test Automation Attributes

This project uses `cy-data` HTML attributes for Cypress/test automation selectors. The attribute name is **`cy-data`** (not `data-cy`).

### Pattern for Adding cy-data

There are two patterns used to apply `cy-data` attributes:

1. **JSX spread pattern** (most common — used in plugins):
   ```jsx
   {...(dataCy ? { 'cy-data': dataCy } : {})}
   // or with a default:
   {...(dataCy ? { 'cy-data': dataCy } : { 'cy-data': 'default-value' })}
   ```

2. **DOM post-render pattern** (for rjsf/third-party forms where you can't control JSX):
   ```jsx
   element.setAttribute('cy-data', cyDataValue);
   ```

### How dataCy Flows Through the System

`dataCy` values are configured in **`static/mapstore/configs/localConfig.json`** as plugin properties, passed through the plugin system as React props, and rendered as `cy-data` HTML attributes.

Example flow: `localConfig.json` → `ActionNavbar` plugin config → `ActionNavbarMenuItem` component → `cy-data` attribute on `<li>`.

### Component → File Mapping (Where UI Elements Are Created)

#### Page Layout & Structure
| UI Element | Source File |
|---|---|
| Main catalogue page shell | `templates/geonode-mapstore-client/catalogue.html` |
| Top brand navbar (logo, menus) | `templates/.../snippets/brand_navbar.html` (server-rendered Django) |
| Header wrapper | `templates/.../snippets/header.html` |
| Footer | `templates/.../snippets/footer.html` |
| Search bar | `templates/.../snippets/search_bar.html` |
| Hero section | `templates/.../snippets/hero.html` |
| Viewer layout (header/left/center/right/footer) | `js/components/ViewerLayout/ViewerLayout.jsx` — classes: `gn-viewer-layout`, `gn-viewer-left-column`, `gn-viewer-right-column` |
| Router/app root | `js/components/Router/Router.jsx` |
| Loading spinner | `js/components/MainLoader/MainLoader.jsx` — class: `gn-main-loader` |
| Error fallback screen | `js/components/ErrorFallback/ErrorFallback.jsx` |
| Event/info messages | `js/components/MainEventView/MainEventView.jsx` — class: `gn-main-event-container` |

#### Resource Catalog (Grid of Cards)
| UI Element | Source File |
|---|---|
| Resource card grid container | `node_modules/mapstore/.../ResourcesCatalog/components/ResourcesContainer.jsx` — cy-data: `dataset-card-{idx}` |
| Individual resource card | `node_modules/mapstore/.../ResourcesCatalog/components/ResourceCard.jsx` — supports `dataCy`, `cardCyPrefix` props → `cy-data` on card, `{prefix}-link`, `{prefix}-title` |
| Resource card action buttons | `node_modules/mapstore/.../ResourcesCatalog/components/ResourceCardActionButtons.jsx` — `{cardCyPrefix}-actions` |
| Menu items (dropdowns) | `node_modules/mapstore/.../ResourcesCatalog/components/MenuItem.jsx` — passes `dataCy` through |
| Favorites button (heart icon) | `js/plugins/Favorites/containers/Favorites.jsx` |
| Compact catalog (modal select) | `js/components/ResourcesCompactCatalog/ResourcesCompactCatalog.jsx` — class: `gn-resources-catalog` |

#### ActionNavbar (Toolbar at Top of Viewer)
| UI Element | Source File |
|---|---|
| Action navbar container | `js/plugins/ActionNavbar/index.jsx` — cy-data: `dataset-view-navbar` |
| Navbar menu items (buttons) | `js/plugins/ActionNavbar/index.jsx` (`ActionNavbarMenuItem`) — props: `dataCy` → `cy-data` |
| Fullscreen button | `js/plugins/ActionNavbar/buttons.jsx` (`FullScreenActionButton`) |
| Layer download button | `js/plugins/ActionNavbar/buttons.jsx` (`LayerDownloadActionButton`) |

#### Resource Details Side Panel
| UI Element | Source File |
|---|---|
| Details panel container | `js/plugins/ResourceDetails/containers/DetailsPanel.jsx` |
| Details header (title + close) | `node_modules/mapstore/.../ResourcesCatalog/components/DetailsHeader.jsx` — cy-data: `dataset-view-sidepanel-close`, `dataset-view-sidepanel-title` |
| Details info tabs | `node_modules/mapstore/.../ResourcesCatalog/components/DetailsInfo.jsx` — cy-data on tab titles and `dataset-view-sidepanel-tab-content` |
| Details toolbar (copy URL, download) | `js/plugins/ResourceDetails/components/DetailsToolbar.jsx` — auto-assigns cy-data based on glyph: `dataset-view-sidepanel-btn-download`, `dataset-view-sidepanel-btn-copy-url`, `dataset-view-sidepanel-btn-copy-ogc` |
| Details thumbnail | `js/plugins/ResourceDetails/components/DetailsThumbnail.jsx` |
| Details preview (expandable iframe) | `js/plugins/ResourceDetails/components/DetailsPreview.jsx` |
| Resource details show button | `js/plugins/ResourceDetails/ResourceDetails.jsx` — supports `dataCy` prop |

#### Save / SaveAs / Delete
| UI Element | Source File |
|---|---|
| Save button | `js/plugins/Save.jsx` — cy-data: `save-button` (default) or custom `dataCy` |
| SaveAs button | `js/plugins/SaveAs.jsx` — cy-data: `btn-save-as` |
| Save modal dialog | `js/plugins/save/SaveModal.jsx` — cy-data: `dataset-save-box`, `dataset-save-box-close-top`, `dataset-save-box-close`, `dataset-save-box-save`, `dataset-save-box-input` |
| Delete confirmation dialog | `js/plugins/DeleteResource.jsx` — supports `dataCy` prop |
| Download button | `js/plugins/DownloadResource.jsx` — supports `dataCy` and `cy-data` props |
| ISO download button | `js/plugins/downloads/IsoDownload.jsx` — supports `dataCy` prop |
| Sync button | `js/plugins/Sync.jsx` |

#### Metadata Editor
| UI Element | Source File |
|---|---|
| Metadata editor form (rjsf) | `js/plugins/MetadataEditor/containers/MetadataEditor.jsx` — uses `metadataCyById` map + `applyMetadataCyTags()` to set cy-data on form fields by DOM ID (e.g., `root_title` → `metadata-edit-title`) |
| Metadata update button | `js/plugins/MetadataEditor/containers/MetadataUpdateButton.jsx` — cy-data: `metadata-edit-update` |
| Metadata back button | `js/plugins/MetadataEditor/index.js` — cy-data: `metadata-edit-back` |
| Metadata viewer modal | `js/plugins/MetadataEditor/MetadataViewer.jsx` — cy-data: `metadata-edit-view` (default) |
| Custom form widgets | `js/plugins/MetadataEditor/components/_widgets/` (TextareaWidget, SelectWidget, RichTextEditor) |
| Custom form templates | `js/plugins/MetadataEditor/components/_templates/` (FieldTemplate, TitleFieldTemplate, ArrayFieldTemplate, ObjectFieldTemplate) |

#### Upload / Operations
| UI Element | Source File |
|---|---|
| Upload panel (drag & drop) | `js/plugins/Operation/components/UploadPanel.jsx` — cy-data: `btn-select-files`, `btn-add-from-url`, `upload-card-{idx}`, `btn-upload` |
| Pending upload file card | `js/plugins/Operation/components/PendingUploadFile.jsx` — cy-data: `{prefix}-title`, `{prefix}-btn-remove`, `{prefix}-ul-filetype`, `{prefix}-size` |
| Execution request table | `js/plugins/Operation/components/ExecutionRequestTable.jsx` — cy-data: `upload-list-{idx}`, `upload-list-{idx}-title`, `upload-list-{idx}-created`, `upload-list-{idx}-processing`, `upload-list-{idx}-btn-view`, `upload-list-{idx}-btn-remove` |
| Operation button | `js/plugins/Operation/index.jsx` — supports `dataCy` prop |

#### Export / Download Dialog
| UI Element | Source File |
|---|---|
| Export dialog | `node_modules/mapstore/.../components/data/download/DownloadDialog.jsx` — cy-data: `dataset-export-box`, `dataset-export-box-close`, `dataset-export-box-export` |
| Format select | `node_modules/mapstore/.../components/data/download/DownloadOptions.jsx` — cy-data: `dataset-export-select-format` |
| Advanced options (SRS) | `node_modules/mapstore/.../components/data/download/DownloadWPSOptions.jsx` — cy-data: `dataset-export-toggle-advanced`, `dataset-export-select-reference-system` |

#### Media Viewers
| UI Element | Source File |
|---|---|
| Media viewer wrapper | `js/plugins/MediaViewer.jsx` — class: `gn-media-viewer` |
| Media type router | `js/components/MediaViewer/Media.jsx` |
| PDF viewer | `js/components/MediaViewer/PdfViewer.jsx` — class: `gn-pdf-viewer` |
| 3D scene viewer (Cesium) | `js/components/MediaViewer/Scene3DViewer.jsx` |
| Spreadsheet viewer | `js/components/MediaViewer/SpreadsheetViewer.jsx` — class: `gn-csv-viewer` |
| Metadata preview iframe | `js/components/MetadataPreview/MetadataPreview.jsx` — class: `gn-metadata-preview` |

#### Other Plugins
| UI Element | Source File |
|---|---|
| Legend panel | `js/plugins/Legend.jsx` — class: `gn-legend-wrapper` |
| Map viewers catalog modal | `js/plugins/MapViewersCatalog.jsx` |
| Map viewer configuration dialog | `js/plugins/MapViewerConfiguration.jsx` |
| Layer detail viewer button + panel | `js/plugins/LayerDetailViewer.jsx` |
| Create dataset form | `js/plugins/CreateDataset/containers/CreateDataset.jsx` |
| Execution tracker | `js/plugins/ExecutionTracker/index.jsx` |

### dataCy Values in localConfig.json

`dataCy` values for ActionNavbar items are defined in `static/mapstore/configs/localConfig.json`. Current values include:
- `dataset-view-navbar-properties`, `dataset-view-navbar-resource-save`, `dataset-view-navbar-resource-save-as`, `dataset-view-navbar-resource-delete`
- `dataset-view-navbar-view-metadata`, `dataset-view-navbar-edit-upload-style`, `dataset-view-navbar-edit-upload-metadata`, `dataset-view-navbar-edit-upload-dataset`
- `dataset-view-navbar-download-dataset`, `dataset-view-navbar-download-export`, `dataset-view-navbar-download-iso-metadata`
- `dataset-view-navbar-share`, `dataset-view-navbar-filter`
- Dropdown toggle: `cy-data` attribute set via `toggleAttributes` (e.g., `dataset-view-navbar-resource`)

## Common Pitfalls

1. **Wrong Node version:** Using Node > 20 causes build failures. Always use `nvm use 20.13.1`.
2. **Missing submodule:** If `MapStore2/` is empty, `npm install` will fail because `package.json` has `"mapstore": "file:MapStore2"`. Always run `git submodule update --init --recursive`.
3. **Forgetting `npm install`:** Always run `npm install` before lint, test, or compile — especially after any `package.json` or submodule changes.
4. **Compiled output is committed:** The `static/mapstore/` directory contains build artifacts that are committed to the repo. After `npm run compile`, new/changed files in `static/mapstore/` must be committed.
5. **`legacy-peer-deps=true`:** Set in `.npmrc`. Do not remove — npm install will fail without it due to peer dependency conflicts.

## Trust These Instructions

These instructions have been validated by running the commands in this repository. Only search for additional information if these instructions are incomplete or produce unexpected errors.
